# QA 체크리스트

테스트몰(시크릿 창, 로그아웃 상태)에서 실제 스토어프론트 URL로 접속해 아래 항목을
하나씩 확인한 결과입니다.

> 비고 열에 각 항목의 구현 방식과 확인 방법을 적어두었습니다.
> 완료 체크(☑)는 테스트몰 상세페이지에서 실제 동작을 확인한 결과이며,
> 5·6번은 관리자 품목 설정 대조와 장바구니·구매 흐름 통과까지 확인했습니다.

| # | 요구사항 | 완료 | 비고 |
| --- | --- | --- | --- |
| 1 | 상품 상세페이지에 골라담기 형태의 커스텀 옵션 UI가 정상 노출된다 | ☑ | `#pickOptionRoot` 에 카드 4종 자동 렌더링. 미노출 시 README §5 콘솔 메시지 확인 |
| 2 | 커스텀 옵션 선택 시 카페24 기본 텍스트버튼 옵션의 선택 상태와 구매 흐름이 함께 갱신된다 | ☑ | 선택완료 클릭 → **해당 텍스트버튼을 네이티브 `click()`** → 카페24가 버튼 선택 상태 갱신 + 선택상품 행 생성까지 자체 처리 (셀렉트형 스킨이면 `value`+`change` 로 자동 전환). `PickOption.diagnose()` 의 "조작방식" 열로 확인 |
| 3 | 옵션 선택 후 선택상품 목록이 정상 생성되고, 반복 클릭·재선택 시 오류나 중복이 생기지 않는다 | ☑ | 담기 처리 중 busy 잠금 + **해당 옵션값이 담긴 행을 직접 확인**하는 폴링으로 연타 시 1행만 생성. 중첩 행(tr 안 li) 이중 계수 방지. 여러 개입수 조합(10+30+50 등) 교차 확인 |
| 4 | 옵션값 suffix(_1, _2) 기준으로 개입수별 추가 가능 횟수가 제한된다 | ☑ | 30개입 2회 담기 → 카드 `is-maxed`(반투명) + 재클릭 시 안내 토스트. 목록에서 행 삭제 시 자동 해제(MutationObserver) |
| 5 | 개입 수별 금액/추가금액이 의도대로 계산되고, 옵션값 수만큼 상품 품목이 모두 생성되어 있다 | ☑ | 금액 계산은 카페24 품목/추가금액 그대로 사용. 관리자 옵션값(_1.._n) 전부 품목 생성·추가금액 일치 여부를 관리자 화면과 대조 |
| 6 | 수량 변경, 총금액 계산, 장바구니 담기, 바로 구매가 정상 동작한다 | ☑ | 해당 로직은 재구현 없이 카페24 기본 사용. UI는 기본 옵션 버튼 클릭(또는 select change)만 발생시킴 — 행 생성 이후 흐름은 기본 스킨과 동일해야 함. ⚠️ 스킨 템플릿 적용 시 구매 버튼의 원본 `onclick` 과 `<form>` 유지 필수 |
| 7 | 선택된 옵션의 활성화 상태가 사용자에게 명확히 표시된다 | ☑ | 선택 카드: 오렌지 테두리+배경+라디오 채움(`is-active`), 소진 카드: `is-maxed`. 콘솔 `PickOption.getState()` 로 상태 교차 확인 |
| 8 | 옵션별 뱃지·설명·추천 문구·추가 가능 횟수·가격 표시 등 변경 가능한 데이터가 외부 설정 JS로 분리되어 있다 | ☑ | 전부 `option_config.js` 로 분리(README §3 문서화). 로직 파일 수정 없이 값 변경 가능 |
| 9 | PC/모바일 반응형에서 옵션 UI 레이아웃이 깨지지 않는다 | ☑ | 768px 미만에서 모바일 스타일 + 바텀시트 모드. 360px 이하 가격줄 세로 배치로 깨짐 방지. 시트는 `mobile.bottomOffset` 으로 구매바와 미겹침 |

## 디버깅 팁

브라우저 콘솔에서 아래 명령으로 상태를 바로 확인할 수 있습니다.

```js
PickOption.diagnose()   // ★ 연동이 안 될 때 가장 먼저 실행
// 인식된 옵션값 목록 + 조작방식(버튼 click / select+change) + 목록 컨테이너 출력

PickOption.getState()
// { qty10: { label: '10개입', used: 1, max: 2 }, ... }

PickOption.rescan()     // 수동으로 강제 재동기화
```

카드가 아예 렌더링되지 않으면 콘솔에 `[pick-option] #pickOptionRoot 컨테이너가 없습니다`
또는 `option_config.js 가 먼저 로드되어야 합니다` 에러가 있는지 확인하세요. 스니펫의
CSS/JS 로드 순서(`option_config` → `pick_util` → `cafe24_bridge` → `pick_option` →
`page`, 5개 전부) 및 `#pickOptionRoot` 삽입 위치를 다시 확인하면 됩니다.

### 담기가 동작하지 않을 때 (`카페24 옵션 컨트롤을 찾지 못했습니다`)

골라담기 UI 는 카페24 기본 옵션을 **대체하지 않고 조작**합니다. 따라서 페이지에
카페24 기본 옵션 DOM 이 실제로 존재해야 합니다. 순서대로 확인하세요.

1. `PickOption.diagnose()` → "인식된 옵션값 수" 가 0 인가?
2. `document.querySelectorAll('.xans-product-option').length` 가 0 이면
   → **스킨에 기본 옵션 모듈 블록이 없습니다.** `html/detail.html` 통합본의
   `module="product_option"` 테이블이 통째로 들어가 있는지 확인하세요
   (`.pd-cafe24-option` 은 숨김용 클래스이며 DOM 은 살아 있어야 합니다).
3. 옵션값이 `30개입_1` 형태(suffix 포함)로 등록되어 있는가?
4. `option_config.js` 의 `counts[].label` 이 suffix 앞부분과 정확히 일치하는가?

## 미구현 항목 / 제한 사항 (제출 시 이 섹션도 함께 기재)

- **로컬 자동 테스트(jsdom)는 두지 않습니다.** 초기에는 목업 DOM 기반 테스트를
  운용했으나, 목업이 실제 카페24 스킨이 렌더한 DOM 과 갈라지면서 "테스트는 통과하는데
  몰에서는 동작하지 않는" 상태를 만들어 제거했습니다. 대신 **변경분을 테스트몰에
  올려 상세페이지에서 직접 확인**하는 방식으로 검증합니다 (경위: [docs/CONTEXT.md](docs/CONTEXT.md)).
  - 검증 수단은 콘솔의 `PickOption.diagnose()` 입니다. 스크립트·CSS 로드, 구매 폼
    래퍼(`product_detail`), 옵션 컨트롤 탐지, 선택상품 목록 컨테이너, 본문 클릭
    차단 여부를 ✅/❌ 로 판정하고, 인식된 옵션값과 조작방식(버튼 `click()` /
    `select`+`change`)을 표로 출력합니다.
  - 위 표의 9개 항목은 이 진단과 실제 화면 조작으로 확인했습니다.
  - 옵션 컨트롤은 옵션값 텍스트로 자동 탐지하므로 대부분의 스킨에서 셀렉터 수정 없이
    동작하지만, 구조가 특이한 스킨은 `option_config.js > cafe24` 를 1회 조정해야 할
    수 있습니다 (README §3 참고).
- `html/detail.html` 통합본에서 **세트상품/추가구성상품 모듈과 하단
  `product_additional`(상세·구매안내·후기·Q&A·관련상품) 블록은 원본 스킨 내용을
  그대로 이어붙여야 합니다.** 파일 안에 위치를 표시해 두었습니다.
- 구매 폼은 카페24가 `<div module="product_detail">` 을 감싸 자동 생성하므로,
  옵션·수량·선택상품목록·구매버튼은 반드시 이 div 안쪽에 있어야 합니다.
  바깥으로 빼면 옵션값이 전송되지 않아 QA 6 이 실패합니다.
- 맛 구성 문자열을 주문 데이터에 남기려면 카페24 텍스트형 "추가입력" 옵션이 필요합니다.
  없을 경우 선택상품 행에 화면 표시만 되고 주문서에는 개입수 옵션값만 남습니다.
- Npay 등 외부 간편결제 버튼은 자체 스크립트로 동작할 수 있어 "옵션 미선택 토스트"
  가드가 적용되지 않을 수 있습니다. 이 경우에도 카페24 기본 검증(옵션 필수)은 동작합니다.
- 선택상품 행의 `_1`, `_2` suffix 는 화면 텍스트만 정리해 감추며(DOM/이벤트 유지),
  주문서·관리자 화면에는 원본 옵션값이 그대로 표시됩니다.
