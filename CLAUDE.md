# CLAUDE.md

카페24 상품 상세페이지 커스텀 옵션 UI("골라담기") 과제 저장소입니다.

## 프로젝트 개요

카페24 기본 상품 상세 스킨 위에 커스텀 옵션 선택 UI를 얹는 프론트엔드 패키지입니다.
구매·장바구니·금액 계산은 **전부 카페24 기본 로직에 위임**하고, 이 UI는 카페24 기본
옵션 요소를 대신 조작하는 역할만 합니다.

| 경로 | 역할 |
| --- | --- |
| [js/option_config.js](js/option_config.js) | 옵션 UI 외부 설정값 (개입수·맛·문구·셀렉터·모바일 동작) |
| [js/pick_util.js](js/pick_util.js) | 공용 헬퍼 (셀렉터 탐색·치환·DOM 생성) |
| [js/cafe24_bridge.js](js/cafe24_bridge.js) | 카페24 연동 전담 (옵션값 파싱·컨트롤 조작·행 관측) |
| [js/pick_option.js](js/pick_option.js) | 골라담기 상태 + 렌더링 |
| [js/page.js](js/page.js) | 탭 전환, 모바일 바텀시트, 합계 표시 동기화 (읽기 전용) |
| [css/pick_option.css](css/pick_option.css) | 옵션 UI 스타일 |
| [css/custom_detail.css](css/custom_detail.css) | 페이지 전체 스타일 |
| [html/detail.html](html/detail.html) | 전체 스킨 템플릿 (카페24 모듈 보존) |
| [html/snippet_detail_pc.html](html/snippet_detail_pc.html), [html/snippet_detail_mobile.html](html/snippet_detail_mobile.html) | 옵션 영역만 이식하는 삽입 스니펫 |

로드 순서는 반드시 `option_config.js → pick_util.js → cafe24_bridge.js →
pick_option.js → page.js` 입니다 (5개 전부).

## 작업 원칙

### 1. 과제용 테스트몰에서만 작업합니다

- 실제 **운영몰에는 어떤 변경도 적용하지 않습니다.**
- 스킨 편집·파일업로더 작업은 과제용 테스트몰 계정에서만 수행합니다.
- 운영몰 접근이 필요해 보이는 요청이 있으면 진행하지 말고 먼저 확인을 요청합니다.

### 2. 카페24 기본 구매/장바구니 로직을 재구현하지 않습니다

- 옵션 선택 → 품목 결정 → 선택상품 목록 추가 → 금액 계산 → 장바구니/바로구매까지의
  흐름은 **카페24가 처리**합니다. 이 프로젝트는 기본 옵션 요소를 클릭하거나
  `<select>` 값을 바꾸고 `change` 이벤트를 발생시키는 방식으로만 개입합니다.
- 기본 옵션 영역은 **`display:none` 으로 숨기는 것까지만** 허용됩니다.
- 기능에 필요한 DOM·이벤트는 **절대 삭제하지 않습니다.** 특히:
  - `<div module="product_detail">` 구매 폼 래퍼 — 이 밖으로 옵션/수량/구매 버튼을
    빼면 옵션값이 전송되지 않습니다.
  - 기본 옵션 `<select>` / 텍스트버튼 (`.pd-cafe24-option` 내부)
  - `#totalProducts` 선택상품 목록과 그 안의 빈 `<tbody>`, "상품이 추가되는 영역" 주석
  - 수량 입력, 총금액 표시, 구매 버튼 영역
- 커스텀 구매바 버튼은 `data-proxy` 로 카페24 원본 버튼을 대신 클릭합니다.
  자체 폼 전송이나 주문 API 직접 호출을 구현하지 않습니다.

### 3. 외부 설정 JS의 역할 범위

- [js/option_config.js](js/option_config.js) 는 **옵션 UI 구성값 관리 전용**입니다.
  개입수 카드, 맛 목록, 문구, 뱃지, 연동 셀렉터, 모바일 동작 설정만 담습니다.
- 설정 JS에 **구매 로직을 대체하는 코드를 넣지 않습니다.** (주문 전송, 금액 계산,
  재고 판정 등은 설정 파일의 책임이 아닙니다.)
- 설정 JS는 **다른 작업자가 값만 바꿔도 동작하도록** 구조와 각 키의 의미를
  주석·문서로 유지합니다. 새 설정 키를 추가하면 파일 내 주석과 [README.md](README.md)
  의 설정 항목 표를 함께 갱신합니다.
- `counts[].label` / `maxAdd` / `price` 는 카페24 관리자 옵션값·품목 설정과
  일치해야 합니다. 표시 가격은 화면용이고 결제 기준은 카페24 품목입니다.

### 4. 옵션과 무관한 영역은 수정하지 않습니다

- 상품 이미지·갤러리, 상세 설명, 배너, 프로모션바, 구매 버튼 외형 등은
  요청받지 않는 한 건드리지 않습니다.
- 스킨 템플릿을 편집할 때도 옵션 UI 동작에 필요한 최소 범위로 제한합니다.

### 5. 민감 정보는 제출물에 포함하지 않습니다

- 카페24 계정 정보, 관리자 비밀번호, API 키/시크릿, 액세스 토큰, 몰 ID 등
  민감 정보를 코드·주석·문서·커밋 메시지에 남기지 않습니다.
- 필요한 경우 `YOUR_MALL_ID` 같은 플레이스홀더를 사용합니다.
- 커밋 전 새로 추가된 값 중 민감 정보가 없는지 확인합니다.

### 6. 평가 기준: 구조 이해 > 디자인 재현

- 픽셀 단위 디자인 재현보다 **카페24 구조를 정확히 이해하고 기능이 안정적으로
  동작하는 것**이 우선입니다.
- 스킨마다 다른 DOM에 대응하도록, 하드코딩된 셀렉터 대신 설정의 후보 셀렉터
  배열(첫 번째로 존재하는 것을 사용)을 유지합니다.
- 기본 스킨의 까다로운 지점(다중 `<tbody>`, 관심상품 버튼이 장바구니와 같은 클래스를
  쓰는 문제 등)은 우회하지 말고 정면으로 처리하고, **왜 그렇게 했는지를 코드 주석에
  남깁니다.** 자동 테스트가 없으므로 이 주석이 회귀를 막는 유일한 장치입니다.

## 코드 스타일

- 비슷한 로직이 반복되면 공통 모듈/헬퍼로 추출합니다.
- 장황한 단일 함수보다 적당한 단위로 분리하되, 과도한 추상화는 피합니다.
- 검증·디버깅용으로 만든 임시 로직·모듈·주석은 **작업 완료 시 전부 삭제**합니다.
- 주석·문구는 한국어로 작성합니다 (기존 코드와 동일).

상세 규칙 — 코드를 수정하기 전에 해당 파일을 읽습니다:

- [.claude/rules/code-structure.md](.claude/rules/code-structure.md) —
  공통화 기준, 분리 기준, 설정/로직 경계, 카페24 연동 코드 규칙
- [.claude/rules/cleanup.md](.claude/rules/cleanup.md) —
  임시 코드·파일·주석 삭제 기준, 유지해야 할 것, 완료 전 체크리스트

## 스킬

| 스킬 | 사용 시점 |
| --- | --- |
| `refactor-common` | 중복 로직을 공통 모듈로 묶거나 긴 함수를 분리할 때 |
| `cleanup-temp` | 기능 구현·디버깅을 끝내고 커밋/제출하기 전 |

`js/` 아래 코드를 수정한 작업은 **완료 보고 전에 `cleanup-temp` 를 거칩니다.**

## 검증

**로컬 자동 테스트는 두지 않습니다.** 목업 DOM 기반 jsdom 테스트를 운용했다가,
목업이 실제 카페24 스킨의 렌더 결과와 갈라져 "테스트는 통과하는데 몰에서는
동작하지 않는" 상태를 만들어 제거했습니다. `test/`, `preview.html`, `demo.html`
을 **새로 만들지 않습니다** (경위: [docs/CONTEXT.md](docs/CONTEXT.md)).

`js/` 를 수정했다면 다음 순서로 확인하고 결과를 그대로 보고합니다.
실패를 숨기지 않습니다.

```bash
node --check js/pick_option.js   # 문법 확인 (수정한 파일 전부)
git diff                         # 자동 테스트가 없으므로 diff 리뷰가 안전망
```

그다음 변경분을 테스트몰에 업로드하고 상세페이지 콘솔에서 확인합니다.

```js
PickOption.diagnose()   // 로드·연동·목록 컨테이너·본문 클릭을 ✅/❌ 로 판정
```

`diagnose()` 의 6개 점검이 전부 ✅ 여야 하며, 여기에 더해 실제로 담기·중복
방지·소진 처리·구매 흐름을 화면에서 조작해 확인합니다
(항목: [QA_CHECKLIST.md](QA_CHECKLIST.md)).
